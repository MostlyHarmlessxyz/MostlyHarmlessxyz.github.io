(() => {
  // ns-params:@params
  var params_default = { defaultTheme: "auto" };

  // <stdin>
  (function() {
    const localStorage = window.localStorage;
    if (!localStorage) {
      return;
    }
    let isDark = false;
    const themeUsed = localStorage.getItem("theme");
    if (themeUsed) {
      isDark = themeUsed === "dark";
    } else {
      isDark = params_default.defaultTheme === "auto" ? window.matchMedia("(prefers-color-scheme: dark)").matches : params_default.defaultTheme === "dark";
    }
    isDark && (document.documentElement.dataset.theme = "dark");
  })();
})();
